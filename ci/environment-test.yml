# This is an addition to either ci/environment.yml or AB_environments/AB_*.conda.yaml,
# adding dependencies specific to this test suite.
channels:
  - conda-forge
dependencies:
  # Testing dependencies
  - alembic
  - altair
  - conda
  - filelock
  - jinja2
  - packaging
  - pytest
  - pytest-timeout
  - pytest-xdist
  - pyyaml
  # FIXME: Do not upgrade until 0.10.0 Windows build is released on conda-forge (#1418) 
  - python-duckdb ==0.9.2
