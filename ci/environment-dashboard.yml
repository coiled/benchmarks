channels:
  - conda-forge
dependencies:
  - python =3.10
  - sqlalchemy =2.0
  - altair =5.0
  - bokeh =3.2
  - panel =1.2
  - pandas =2.0
  - tabulate =0.9

  # These imports are only needed to parse the source code of the tests and embed it in
  # the dashboard
  - coiled
  - conda
  - dask
  - dask-expr
  - dask-ml
  - distributed
  - filelock
  - optuna
  - pytest
  - s3fs
  - xarray
  - xgboost
  - zarr
