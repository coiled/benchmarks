channels:
  - conda-forge
  - pytorch
dependencies:
  - jinja2
  - optuna
  - packaging
  - pytest
  - pytest-xdist
  - python
  - pytorch
  - pyyaml
  - sqlalchemy
  - alembic
  # Temporary workaround for `filelock=3.11.0` causing package sync to deadlock
  - filelock=3.10.7
  - cftime
  - conda
